To simply run the main.py file, we could get the result of dataset20. You could change the variable "dataset" in main.py to switch the dataset (remember to set the "TEXTURE" to False when use dataset23).

"COUNT": determine the number of particles

This python file main take 30mins to obtain a result with texture.